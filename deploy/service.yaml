apiVersion: v1
kind: Service
metadata:
  name: gatehouse
  namespace: gate
  labels:
    app.kubernetes.io/name: gatehouse
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: gatehouse
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
